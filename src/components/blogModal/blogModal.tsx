import React from "react";

import "./blogModal.scss";

type blogModalProp = {
    previewImg: string,
    publishedDateTime: Date,
    title: String,
    tag: String, 
}

export default function blogModal({previewImg, publishedDateTime, title, tag}: blogModalProp) {
    
}